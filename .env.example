# Vazio usa o proxy do Vite para http://localhost:8000/api/v1
# Em hospedagem estática, informe a origem da API e configure CORS no backend.
VITE_API_URL=

# Deve usar o mesmo host do callback cadastrado no GitHub OAuth App.
VITE_OAUTH_URL=http://localhost:8000
